# Type hint tests

These files are **not** meant to be executed.
They only exercise the type interface and are checked by our supported type checkers.

The directory is an importable package so files like `flask.py` don't shadow the real Flask.
